#compdef rns-page-node

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _rns-page-node (e.g. ~/.local/share/zsh_completion/_rns-page-node).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_rns_page_node_commands() {
  local _commands=(
    
  )
  _describe 'rns-page-node commands' _commands
}

_shtab_rns_page_node_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-c,--config}"[Reticulum config path]:configpath:"
  {-p,--pages-dir}"[Pages directory]:pages_dir:"
  {-f,--files-dir}"[Files directory]:files_dir:"
  {-m,--media-dir}"[Media directory for images, defaults to pages directory]:media_dir:"
  {-n,--node-name}"[Node display name]:node_name:"
  {-a,--announce-interval}"[Announce interval in minutes]:announce_interval:"
  {-i,--identity-dir}"[Directory to store node identity]:identity_dir:"
  "--page-refresh-interval[Page refresh interval in seconds, 0 disables auto-refresh]:page_refresh_interval:"
  "--file-refresh-interval[File refresh interval in seconds, 0 disables auto-refresh]:file_refresh_interval:"
  {-l,--log-level}"[Logging level]:log_level:(DEBUG INFO WARNING ERROR CRITICAL)"
  ":Path to rns-page-node config file:"
)

# guard to ensure default positional specs are added only once per session
_shtab_rns_page_node_defaults_added=0


_shtab_rns_page_node() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->rns-page-node'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_rns_page_node_defaults_added )); then
    if (( ${_shtab_rns_page_node_options[(I)${(q)one_or_more}*]} +          ${_shtab_rns_page_node_options[(I)${(q)remainder}*]} +          ${_shtab_rns_page_node_options[(I)${(q)default}]} == 0 )); then
      _shtab_rns_page_node_options+=(': :_shtab_rns_page_node_commands' '*::: :->rns-page-node')
    fi
    _shtab_rns_page_node_defaults_added=1
  fi
  _arguments -C -s $_shtab_rns_page_node_options

  case $state in
    rns-page-node)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_rns_page_node-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_rns_page_node -N rns-page-node
else
  # autoload from fpath, call function directly
  _shtab_rns_page_node "$@"
fi
